root kasutajana:
installi - apt install git
tavakasutajana:
esmakordselt:

a) seadista - maara kasutajanimi, emal, editor
git config --global user.name "Katriin Poska"
git config --global user.email katriin.poska@voco.ee
git config --global core.editor nano

b) loo kataloog, kus hakkad kriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorium git süsteemi jaoks antud kaustas
git init

remote seadistamine:
e) git remote add nimi github:address
git remote add skriptimine git@github.com:Katzz11/skriptimine.git

f)git remote kontroll
git remote -v
